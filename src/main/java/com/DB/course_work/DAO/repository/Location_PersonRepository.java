package com.DB.course_work.DAO.repository;

import com.DB.course_work.DAO.entities.Location_Person;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Location_PersonRepository extends JpaRepository<Location_Person, Integer> {
}
